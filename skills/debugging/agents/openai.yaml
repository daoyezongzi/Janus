interface:
  display_name: "Janus Debugging"
  short_description: "Evidence-first diagnosis with recovery"
  default_prompt: "Use $debugging to diagnose unexpected behavior from evidence, recover long investigations when needed, verify the smallest fix, and hand repository changes to $developing."
