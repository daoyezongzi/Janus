interface:
  display_name: "Janus Debugging"
  short_description: "Evidence-first diagnosis with recovery"
  default_prompt: "Use $debugging when observed behavior differs from expectations; keep a durable plan for long investigations and recover it when context is unclear."
